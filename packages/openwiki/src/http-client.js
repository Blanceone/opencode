/**
 * Browser/TUI-facing OpenWiki HTTP client from generated `@opencode-ai/client`.
 * Kept as a separate entry so App can call openwikis.* without replacing its vendored client.
 */
export { OpenCode, ClientError } from "@opencode-ai/client"
